name: unlimited-analytics
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - geopandas
  - pip:
      - dash==2.13.0
      - pandas==2.0.3
      - dash-bootstrap-components==1.5.0
      - basemap
      - dask[complete]
      - datashader==0.16.3
      - basemap
      - statsmodels
      - imblearn
      - Flask-Caching

